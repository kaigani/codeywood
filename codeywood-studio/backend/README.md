To run:

./run.sh

This starts the Flask API on localhost:5555 and Vite dev server on localhost:5173, then opens the browser. Ctrl+C to stop both.

For production mode (pre-built frontend, single server):
./run-prod.sh
